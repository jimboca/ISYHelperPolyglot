<editors>

   <editor id="I_INTEGER">
       <range uom="25" min="0" max="1000" prec="0" />
   </editor>

   <editor id="I_INTEGER_L">
       <range uom="25" min="0" max="4294967295" prec="0" />
   </editor>

   <editor id="BOOL">
       <range uom="2" subset="0,1" nls="B_TF"/>
   </editor>

   <editor id="FLOAT">
       <range uom="56" />
   </editor>

   <editor id="I_ONOFF">
       <range uom="25" subset="0,1" nls="CONOFF" />
   </editor>

    <!-- 16 Position Selector -->
    <editor id="I_POS16">
        <range uom="25" subset="1-16" nls="CPOS" />
    </editor>

    <!-- 0-9 Motion Sensitivity -->
    <editor id="I_ALMOS">
        <range uom="25" subset="0-9" nls="CMOS" />
    </editor>

    <!-- 0-4 Foscam polling mode -->
    <editor id="I_FOSCAM_MJPEG">
        <range uom="25" subset="0-4" nls="CFMJPEG" />
    </editor>

    <!-- 0-4 Log Debug Mode -->
    <editor id="I_DM">
        <range uom="25" subset="0-4" nls="CDM" />
    </editor>

</editors>
